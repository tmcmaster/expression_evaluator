name: expression_evaluator
description: A new Flutter package project.
version: 0.0.1
homepage: https://github.com/tmcmaster/expression_evaluator/blob/main/README.md

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.2.0"

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  expressions:

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic:
  lint:
  test:
  build_runner:
  freezed_annotation:
  json_serializable:
  json_annotation: